// TODO(quelch v2 phase 3+): re-enable when quelch dev TUI snapshot lands.
//
// The tui_snapshot tests run `quelch sim --snapshot-to` which is disabled in Phase 1.
// They will be replaced by `quelch dev` TUI snapshot tests in Phase 3/4.

#[test]
fn placeholder() {
    // This test exists solely to keep the file compiling while sim is stubbed.
    // TODO(quelch v2 phase 3+): replace with quelch dev TUI snapshot tests
}
